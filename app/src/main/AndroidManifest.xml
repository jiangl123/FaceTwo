<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liu04.facetest">

    //github test project two1111
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".InitApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".activity.FaceMatchActivity" />
        <activity android:name=".activity.FaceAddActivity" />
        <activity android:name=".activity.IdentifyActivity" />
        <activity android:name=".activity.DetectActivity" />
        <activity android:name=".activity.FaceManageActivity" />
        <activity android:name=".activity.FaceUpdateActivity" />
        <activity android:name=".activity.FaceQueryActivity" />
        <activity android:name=".activity.FaceDeleteActivity" />
        <activity android:name=".AllActivity"
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/NoTitle"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>